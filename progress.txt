# MPL Build Progress

## Learnings
- Tailwind v4 uses CSS-based `@theme` for custom colors, not just `tailwind.config.ts`. Use `@config` directive in globals.css to link the config.
- `create-next-app` refuses project names with capitals — use temp dir and copy approach
- When copying from `/tmp`, symlinks in `node_modules/.bin/` break — must `rm -rf node_modules && npm install` after copy
- Vitest needs `passWithNoTests: true` in config to avoid exit code 1 when no test files exist yet

---

## Iteration 1 - 1.1 Initialize Next.js project
- Initialized Next.js 16 (App Router) with TypeScript, TailwindCSS v4, ESLint
- Created tailwind.config.ts with MPL brand colors (mpl.primary, mpl.primaryDark, mpl.primaryLight, mpl.accent, mpl.bg, mpl.surface, mpl.border)
- Configured globals.css with @theme inline block for Tailwind v4 color tokens and system font stack
- Created MplHeader component with blue gradient, "M" logo, title "MPL", subtitle "Manual Process Log"
- Set up project structure: app/(main)/, components/, lib/supabase/, types/, supabase/migrations/
- Installed Vitest + testing libraries, configured vitest.config.ts
- Files changed: package.json, tailwind.config.ts, app/globals.css, app/layout.tsx, app/page.tsx, components/MplHeader.tsx, vitest.config.ts
- Learnings for future iterations:
  - Tailwind v4 color tokens use `--color-*` CSS custom properties via `@theme inline`
  - The `@config` directive in globals.css bridges tailwind.config.ts with Tailwind v4
  - System font stack: `-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`
  - Max-width 430px centered is the mobile-first container approach
  - Next.js 16 was installed (latest), which uses Turbopack by default
---

## Iteration 2 - 1.2 Set up Supabase client and types
- Installed @supabase/supabase-js and @supabase/ssr packages
- Created lib/supabase/client.ts using createBrowserClient from @supabase/ssr, typed with Database generic
- Created lib/supabase/server.ts using createServerClient from @supabase/ssr with cookie handling via next/headers
- Created types/database.ts with full TypeScript types for all 4 tables (users, categories, subtasks, log_entries) including Row/Insert/Update variants and Relationships
- Added convenience type aliases: User, UserInsert, Category, Subtask, LogEntry, LogEntryInsert
- Files changed: package.json, lib/supabase/client.ts, lib/supabase/server.ts, types/database.ts
- Learnings for future iterations:
  - @supabase/ssr replaces the old @supabase/auth-helpers-nextjs package
  - Server client needs async cookies() call in Next.js 15+ (cookies is now a promise)
  - The setAll catch block is needed because Server Components can't set cookies — middleware handles session refresh
  - Database types follow Supabase generated format: Row (full), Insert (required fields only), Update (all optional)
  - Convenience aliases like `User`, `Category` etc. make imports cleaner throughout the app
---

## Iteration 3 - 1.3 Create Supabase migration for schema
- Created supabase/migrations/001_initial_schema.sql with all 4 tables
- Tables: users (PK references auth.users), categories, subtasks, log_entries
- All CHECK constraints match PRD: team IN ('CH','MH'), role IN ('member','supervisor'), minutes > 0, occurrences > 0
- All foreign keys: subtasks→categories, log_entries→users/categories/subtasks
- Created 5 indexes: log_entries(user_id), log_entries(category_id), log_entries(created_at), subtasks(category_id), categories(team)
- Files changed: supabase/migrations/001_initial_schema.sql
- Learnings for future iterations:
  - SQL migrations are pure SQL files — no TypeScript compilation needed, just validate with tsc that nothing else broke
  - Schema matches types/database.ts exactly — future iterations can rely on this consistency
  - The users table PK references auth.users(id) — Supabase Auth must be set up before users can be inserted
---

## Iteration 4 - 1.4 Create seed data migration
- Created supabase/migrations/002_seed_categories.sql with all 19 categories and 85 subtasks
- CH: 9 categories (Dwell Report, Routing Actions, Notice Emails, Manual Dispatch, Invoicing, 3rd Party Website, Rail Storage, Report / Macro, Other) with 27 subtasks
- MH: 10 categories (Port / Terminal Mgmt, Routing Actions, Invoicing, Outlook / Notice Emails, Rail Billing, Cross Border, Report / Macro, Transshipment / Barge / Omit, 3rd Party Website, Other) with 58 subtasks
- Used CTE pattern (WITH cat AS ... RETURNING id) to insert category and its subtasks in one statement
- Every single subtask from the PRD taxonomy is included with sequential sort_order values
- Files changed: supabase/migrations/002_seed_categories.sql, PRD.md, progress.txt
- Learnings for future iterations:
  - CTE with RETURNING id is the cleanest pattern for inserting parent + children with gen_random_uuid() PKs
  - SQL seed migrations don't affect TypeScript compilation — just verify with tsc that nothing else broke
  - Categories shared across teams (Routing Actions, Invoicing, 3rd Party Website, Report / Macro, Other) have separate rows per team with team-specific subtasks
---

## Iteration 5 - 1.5 Set up Row Level Security
- Created supabase/migrations/003_rls_policies.sql
- Enabled RLS on all 4 tables: users, categories, subtasks, log_entries
- Users policies: read own row, supervisors read all team members, insert own row (onboarding), update own row
- Categories & subtasks: readable by all authenticated users (auth.uid() IS NOT NULL)
- Log entries: insert own, read own, supervisors read all entries for users on their team
- Supervisor team access uses EXISTS subquery joining users table to check role='supervisor' and matching team
- Files changed: supabase/migrations/003_rls_policies.sql, PRD.md, progress.txt
- Learnings for future iterations:
  - RLS policies use auth.uid() to identify the current user
  - Multiple SELECT policies on the same table are OR'd together — a user sees rows matching ANY policy
  - Supervisor access pattern: EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'supervisor' AND team = target.team)
  - INSERT policies use WITH CHECK (not USING) to validate new rows
  - UPDATE policies need both USING (which rows can be seen) and WITH CHECK (what the new values can be)
---

## Iteration 6 - 1.6 Set up Supabase Auth
- Created login page with magic link auth at app/login/page.tsx
- Created auth callback handler at app/auth/callback/route.ts
- Created middleware.ts for route protection
- Login page: email input, "Send Magic Link" button, MPL branding, success state with "Check your email" message
- Auth callback: exchanges code for session, checks if user exists in users table, redirects to /onboard if not
- Middleware: refreshes session, protects all routes except /login and /auth/callback, redirects unauthenticated users to /login, redirects authenticated users without users table row to /onboard, redirects logged-in users away from /login to /
- Files changed: app/login/page.tsx, app/auth/callback/route.ts, middleware.ts, PRD.md, progress.txt
- Learnings for future iterations:
  - Middleware needs its own Supabase client (not the server.ts one) because it must set cookies on the response object
  - The middleware cookie pattern: getAll reads from request.cookies, setAll writes to both request.cookies and response.cookies
  - Auth callback uses exchangeCodeForSession (PKCE flow) — this is the standard Supabase magic link pattern
  - Middleware matcher excludes static assets and images to avoid unnecessary auth checks
  - Onboarding check in middleware: query users table by auth.uid() — if no row, redirect to /onboard
  - signInWithOtp with emailRedirectTo sends the magic link to the specified callback URL
---

## Iteration 7 - 1.7 Create onboarding flow
- Created app/onboard/page.tsx with name input and team selection (CH/MH)
- Two large team selection buttons: CH (amber styling with 📦) and MH (blue styling with 🚢)
- On submit, inserts row into users table with auth user ID, email, name, and team
- Checks if user already exists in users table on mount — redirects to / if so
- Redirects unauthenticated users to /login
- Loading state while checking existing user, submit button disabled until name + team selected
- Error handling for auth and database errors
- Consistent styling with login page (card layout, MPL branding, same input/button styles)
- Files changed: app/onboard/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - Onboard page is a client component since it handles form state and Supabase client calls
  - The middleware already handles redirecting non-authed users and checking onboarding status
  - The onboard page also does its own check (belt + suspenders) for better UX
  - Team selection uses border-2 with conditional styling for selected vs unselected states
  - router.replace() instead of router.push() so user can't navigate back to onboarding
---

## Iteration 8 - 2.1 Create bottom navigation
- Created components/BottomNav.tsx with 4 tabs: Log (➕), History (📋), Insights (📊), Team (👥)
- Active tab shows MPL blue with top border accent, inactive tabs show slate-400
- Includes safe area padding for iOS via env(safe-area-inset-bottom)
- Created app/(main)/layout.tsx as server component that fetches user profile from Supabase
- Header now shows user initials avatar (circle) and CH/MH team badge (amber for CH, blue for MH)
- Updated MplHeader to accept optional initials and team props (backward compatible)
- Created placeholder pages for all 4 tabs: log, history, insights, team
- Updated app/page.tsx to redirect to /log (since main content lives in (main) route group now)
- Files changed: components/BottomNav.tsx, components/MplHeader.tsx, app/(main)/layout.tsx, app/(main)/log/page.tsx, app/(main)/history/page.tsx, app/(main)/insights/page.tsx, app/(main)/team/page.tsx, app/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - The (main) layout is a server component — fetches user profile and passes initials/team to MplHeader
  - BottomNav is a client component (needs usePathname for active tab detection)
  - Safe area padding uses env(safe-area-inset-bottom) CSS function for iOS notch devices
  - pb-24 on main content prevents bottom nav from overlapping page content
  - Root page.tsx redirects to /log — the (main) route group handles the authenticated shell
  - getInitials helper: takes first letter of first name + first letter of last name
---

## Iteration 9 - 2.2 Build category selection (Step 1 of logging)
- Created the 3-step logging flow page at app/(main)/log/page.tsx as a client component
- Fetches categories with subtasks from Supabase, filtered by user's team
- Displays categories in a 2-column grid with icon, label, and sub-task count
- Tapping a category sets it as selected and advances to step 2 (subtask)
- Breadcrumb navigation with back arrow shows when not on category step
- Skeleton loading state with animated placeholders while data loads
- Error state with styled error message
- Placeholder for subtask step (to be built in task 2.3)
- Files changed: app/(main)/log/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - The log page is a client component managing multi-step state (step, selectedCategory, selectedSubtask)
  - Categories are fetched with `select('*, subtasks(*)')` to get subtask count and data for step 2
  - The CategoryWithSubtasks type extends Category with subtasks array — needed because Supabase joins aren't in the generated types
  - The (main)/layout.tsx doesn't pass user profile to children — each page fetches what it needs from Supabase client
  - active:scale-[0.97] gives a nice press feedback on mobile buttons
  - The flow will grow: task 2.3 adds subtask selection, 2.4 adds time entry, 2.5 adds timer, 2.6 adds submission
---

## Iteration 10 - 2.3 Build sub-task selection (Step 2 of logging)
- Replaced placeholder subtask section with vertical list of tappable subtask rows
- Each row shows subtask label with chevron (›), sorted by sort_order
- Added selectedSubtask state to track the chosen subtask
- Added handleSubtaskSelect function that sets subtask and advances to time step
- Updated breadcrumb to show Category > Sub-task path when on time step
- Breadcrumb category label is clickable (returns to subtask step) when on time step
- Back navigation clears selectedSubtask appropriately at each step
- Added placeholder for Step 3 (time entry) referencing the selected subtask
- Changed breadcrumb category from <span> to <button> for accessibility
- Files changed: app/(main)/log/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - Subtasks are already fetched with categories via `select('*, subtasks(*)')` — no extra query needed for step 2
  - Array.sort() mutates in place — used inline `.sort((a, b) => a.sort_order - b.sort_order)` on render
  - The overflow-hidden on the container with rounded-xl clips the border-radius on child buttons
  - Breadcrumb navigation allows going back to subtask step from time step by clicking category label
---

## Iteration 11 - 2.4 Build time entry with chips (Step 3 of logging)
- Built complete Step 3 time entry UI in app/(main)/log/page.tsx
- Context pill at top showing selected category icon + label + subtask
- Mode toggle: Quick Entry vs Timer (pill toggle with active state)
- Quick Entry mode: 7 time chips (5m, 10m, 15m, 20m, 30m, 45m, 60m) + custom minute input
- Chip selection highlights blue; selecting a chip syncs customMinutes; typing custom value deselects chips unless it matches a chip value
- Toggling a selected chip off clears it (deselect behavior)
- Occurrences stepper: − button, numeric input, + button, "How many times?" helper text
- Optional note textarea
- Submit button: disabled when no time selected, shows "Log 15m Entry" when time selected
- Timer mode: placeholder with 00:00 display (to be built in task 2.5)
- resetTimeEntry() function clears all time entry state when navigating back
- Files changed: app/(main)/log/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - TIME_CHIPS as const tuple enables type-safe .includes() checks
  - Custom input shows value only when it doesn't match a chip (otherwise chips handle display)
  - effectiveMinutes derives from selectedChip ?? parsed customMinutes — clean computed state pattern
  - canSubmit boolean gates the submit button; timer mode will set effectiveMinutes differently in task 2.5
  - The entryMode state controls which input UI is shown (quick vs timer)
  - inputMode="numeric" on inputs gives mobile users a number pad without restricting to type="number" quirks
---

## Iteration 12 - 2.5 Build timer mode
- Replaced timer placeholder with full timer UI in app/(main)/log/page.tsx
- Large monospace MM:SS display (text-5xl font-mono)
- Timer controls: Start button (blue), Pause button (red), Resume button (blue), Reset button (border style)
- Timer runs via setInterval in a useEffect keyed on timerRunning state
- useRef holds the interval ID for proper cleanup
- Start/Pause/Resume/Reset via useCallback handlers
- When paused with time > 0, shows "X minutes will be logged" helper text
- timerMinutes uses Math.ceil(timerSeconds / 60) to round up partial minutes
- effectiveMinutes now derives from timerMinutes when in timer mode
- canSubmit now requires !timerRunning (submit disabled while timer actively running)
- resetTimeEntry() also resets timerRunning and timerSeconds when navigating back
- formatTimerDisplay helper formats seconds to MM:SS with zero-padding
- Files changed: app/(main)/log/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - Timer pattern: useRef for interval ID + useEffect keyed on boolean running state + cleanup in return
  - Math.ceil(seconds / 60) converts seconds to minutes rounding up (so 61 seconds = 2 minutes)
  - useCallback for timer controls prevents unnecessary re-renders of child components
  - Timer state (timerSeconds, timerRunning) is separate from quick entry state — both coexist, entryMode switches which is used
  - The verified field in log_entries will be true for timer entries (timerSeconds > 0) — task 2.6 will implement this
---

## Iteration 13 - 2.6 Implement log entry submission
- Added handleSubmit async function that INSERTs into log_entries via Supabase client
- Entry fields: user_id (from auth session), category_id, subtask_id, minutes (effectiveMinutes), occurrences, note (null if empty), verified (true if timer mode with timerSeconds > 0)
- Added submitting state to prevent double-submission and show "Logging..." on button
- Added toast notification system: showToast() displays a fixed-position toast for 2 seconds with auto-dismiss
- Toast styled with green left-accent bar for success, red for error (matches design spec)
- Added fadeIn CSS keyframe animation in globals.css for toast entrance
- On success: shows "Entry logged ✅", resets all form state (time, occurrences, note, timer), clears selections, returns to category step
- On error: shows error toast, does NOT reset state (user can retry)
- Cleanup: toastTimeoutRef cleared on unmount to prevent memory leaks
- Submit button now: calls handleSubmit onClick, disabled when submitting OR !canSubmit, shows dynamic label
- Files changed: app/(main)/log/page.tsx, app/globals.css, PRD.md, progress.txt
- Learnings for future iterations:
  - Toast pattern: useRef for timeout ID + state for visibility + cleanup on unmount
  - The finally block in handleSubmit always resets submitting state, even on unexpected errors
  - verified flag: entryMode === 'timer' && timerSeconds > 0 — both conditions needed (user could switch to timer but never start it)
  - After successful submission, state resets in order: resetTimeEntry() → clear selections → set step to 'category'
  - The toast persists across step changes since it's rendered at the top of the component, outside step-conditional blocks
---

## Iteration 14 - 3.1 Build history page with date grouping
- Built complete history page at app/(main)/history/page.tsx as a client component
- Fetches user's log_entries from Supabase with JOIN on categories(label, icon) and subtasks(label)
- Orders by created_at DESC
- Groups entries by date using a Map: "Today", "Yesterday", then formatted dates (e.g., "Wed, Feb 12")
- Each date group header shows the label and daily total minutes in MPL blue
- Entry cards show: category icon in blue-tinted square, category label, TIMER/MANUAL badge, subtask name in blue, optional truncated note, timestamp, occurrence count, bold minute display, per-occurrence average when > 1
- TIMER badge: green bg (#ECFDF5) + green text (#065F46); MANUAL badge: amber bg (#FEF3C7) + amber text (#92400E)
- Empty state with icon and helpful message
- Loading skeleton with animated placeholders
- Error state with red styling
- Helper functions: formatDateLabel (Today/Yesterday/formatted), getDateKey (YYYY-MM-DD from timestamp), formatTime (12h AM/PM), groupEntriesByDate (Map-based grouping)
- Files changed: app/(main)/history/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - LogEntryWithRelations type extends LogEntry with categories and subtasks join data
  - Date grouping uses getDateKey to extract YYYY-MM-DD from ISO timestamp, then Map for O(n) grouping
  - formatDateLabel compares dates at midnight to avoid timezone issues (appends T00:00:00)
  - Supabase select('*, categories(label, icon), subtasks(label)') gives nested relation data
  - The history page is read-only — no mutation needed, just fetch and display
  - Tasks 3.2 (entry cards) and 3.3 (summary card) are already partially addressed — cards are built inline, summary card is separate
---

## Iteration 15 - 3.2 Build history entry cards
- Entry cards were already fully implemented in iteration 14 as part of the history page build
- All PRD requirements verified present: category icon in blue-tinted rounded square, category label with TIMER/MANUAL badge, sub-task name in blue, truncated note, clock timestamp, occurrence count (when >1), bold blue minutes, per-occurrence average
- TIMER badge: green bg (#ECFDF5) + green text (#065F46); MANUAL badge: amber bg (#FEF3C7) + amber text (#92400E)
- TypeScript passes, tests pass
- Files changed: PRD.md (marked complete), progress.txt
- Learnings for future iterations:
  - When iteration N builds something ahead of schedule, iteration N+1 should just verify and mark complete rather than rebuilding
  - The history entry card layout: flex row with icon (shrink-0), middle content (flex-1 min-w-0), and minutes (shrink-0 text-right)
  - truncate class on note prevents long notes from breaking the card layout
---

## Iteration 16 - 3.3 Build history summary card
- Added gradient blue summary card at top of history page showing today's totals
- Card shows: "Today's Total" label, large minute count (4xl bold), entry count, total occurrences
- Gradient uses from-mpl-primary to-mpl-primary-dark (blue gradient per PRD)
- Today's stats calculated by filtering entries where getDateKey matches today's date key
- Pluralization handled for "entry/entries" and "occurrence/occurrences"
- Card always shows even when today has 0 entries (displays 0m, 0 entries, 0 occurrences)
- Files changed: app/(main)/history/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - Reuse existing helper functions (getDateKey) for calculating today's filter rather than duplicating logic
  - The summary card renders above the date groups in the same space-y-6 container
  - bg-gradient-to-br with from/to Tailwind utilities creates the blue gradient card
  - text-white/80 and text-white/70 create subtle opacity variants for hierarchy in white-on-blue text
---

## Iteration 17 - 4.1 Build KPI cards
- Built insights page at app/(main)/insights/page.tsx as a client component
- Fetches all user's log_entries from Supabase (no joins needed — only need minutes, occurrences, created_at)
- 2x2 grid of KPI stat cards: Total Minutes (blue), Hours/Week (amber), Total Entries (green), Total Occurrences (red)
- Each card has: icon in tinted rounded square, large bold value with suffix, label text
- Hours/Week calculated from date range span: totalMinutes / 60 / weekSpan, minimum 1 week span
- calculateKpis pure function for easy testability
- Loading skeleton: 4 animated placeholder cards matching 2x2 grid
- Empty state: "Log entries to see insights" message
- Error state: red card with error message
- Files changed: app/(main)/insights/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - Insights page only needs log_entries fields (minutes, occurrences, created_at) — no joins required for KPIs
  - Hours/Week uses Math.max(..., 1) for weekSpan to avoid division issues when all entries are on the same day
  - Card color scheme: blue (#E0F2FE/#0284C7), amber (#FEF3C7/#92400E), green (#ECFDF5/#065F46), red (#FEF2F2/#991B1B)
  - The page wraps content in space-y-6 div — tasks 4.2 and 4.3 will add sections below the KPI grid
  - toLocaleString() formats large numbers with commas for readability
---

## Iteration 18 - 4.2 Build category breakdown chart
- Added "Time by Category" section below KPI cards on insights page
- Updated Supabase query from `select('*')` to `select('*, categories(label, icon)')` to include category join data
- Added LogEntryWithCategory type extending LogEntry with categories relation
- Added CategoryBreakdown type and calculateCategoryBreakdown() pure function
- Aggregates minutes per category using Map, sorts by minutes descending
- Each row: category icon in blue-tinted square, label, total minutes (bold blue), percentage of total
- Horizontal progress bar with blue gradient, relative to highest category (top category = 100% bar width)
- percentage column shows share of total minutes across all categories
- Section wrapped in card with border, only renders when categories have data
- Files changed: app/(main)/insights/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - Two percentage concepts: bar width is relative to max category (visual comparison), text percentage is share of total (absolute contribution)
  - calculateCategoryBreakdown is a pure function — easy to unit test
  - LogEntryWithCategory extends LogEntry with `categories: { label: string; icon: string } | null` for the Supabase join
  - The existing entries state type changed from LogEntry[] to LogEntryWithCategory[] since the query now includes joins
---

## Iteration 19 - 4.3 Build automation opportunity callout
- Added amber "Automation Opportunity" callout card below category breakdown on insights page
- Updated LogEntryWithCategory type to include subtasks join data (subtasks: { label: string } | null)
- Updated Supabase query to fetch subtasks(label) alongside categories(label, icon)
- Added TopSubtask type and calculateTopSubtask() pure function
- Identifies the sub-task with the highest total minutes across all entries
- Shows: sub-task name, parent category (with icon), total minutes, projected monthly savings (total × 4.3)
- Card only renders when topSubtask is non-null (i.e., entries exist)
- Amber styling matches PRD brand: bg #FEF3C7, border amber-200, text #92400E/#78350F, icon bg #FDE68A
- Files changed: app/(main)/insights/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - calculateTopSubtask iterates subtaskMap.values() to find max — avoids sorting overhead for single-max operation
  - Monthly savings projection: total minutes × 4.3 (average weeks per month), using Math.round for clean display
  - The subtask join is added alongside existing category join: select('*, categories(label, icon), subtasks(label)')
  - Amber color palette for the card: #FEF3C7 (bg), #FDE68A (icon bg), #92400E (secondary text), #78350F (primary text)
---

## Iteration 20 - 5.1 Build team overview page
- Built complete team overview page at app/(main)/team/page.tsx as a client component
- Supervisor view: gradient blue aggregate card (total hours this week, member count), CH/MH/All filter pills, member cards list
- Member view: informational card directing to Insights for personal stats
- Team aggregate card: gradient blue, shows weekly total hours and member count, updates when filter changes
- Filter buttons: All/CH/MH pills with active state (blue fill), only relevant for supervisors
- Member cards: initials avatar (blue circle), name, CH/MH team badge (amber/blue), top category with icon, entry count, total minutes (bold blue)
- Data fetching: fetches user profile, team members, and this week's log entries with category joins
- Aggregation in JS: builds per-member stats from entries using Map, finds top category per member by minutes
- Week range calculation: Monday-Sunday using getWeekRange() helper
- Sorted members by total minutes descending
- Loading skeleton: aggregate card + filter pills + member card placeholders
- Error/empty states handled
- Files changed: app/(main)/team/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - RLS enforces team access — supervisors can only see their own team's users and entries
  - The .in('user_id', memberIds) filter fetches entries for all team members in one query
  - getWeekRange() computes Monday 00:00:00 to Sunday 23:59:59 — uses (dayOfWeek + 6) % 7 for Monday-based weeks
  - Filter state is local — just filters the already-fetched memberStats array, no re-fetch needed
  - Team badge colors: CH = amber (#FEF3C7/#92400E), MH = blue (#E0F2FE/#0284C7) — same pattern used in header
  - getInitials helper duplicated from layout.tsx — could be extracted to a shared util but kept local for simplicity
---

## Iteration 21 - 5.2 Build team data queries
- Created server action at app/(main)/team/actions.ts with fetchTeamData() function
- Server action aggregates team data: total minutes per member, top category per member, entry count per member
- Accepts optional dateRange parameter (default: current week) for date range filtering
- RLS enforced via Supabase — supervisors can only see their team's users/entries
- Refactored team page to call server action instead of inline client-side data fetching
- Added date range filter UI: This Week / Last Week / This Month preset buttons
- Date preset changes trigger re-fetch via server action with appropriate date range
- Aggregate card header dynamically shows the selected date range label
- Member cards show "No entries this period" instead of "this week" for generic wording
- Files changed: app/(main)/team/actions.ts (new), app/(main)/team/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - Server actions use 'use server' directive and async function pattern — called directly from client components
  - Server actions use the server Supabase client (lib/supabase/server.ts) which gets cookies automatically
  - getWeekRange(offset) with negative offset gives previous weeks — useful for "Last Week" preset
  - Date preset filter triggers a full re-fetch (new server action call) since entries change per date range
  - Team filter (All/CH/MH) stays client-side since it just filters already-fetched memberStats
  - MemberStats type is exported from actions.ts and imported in page.tsx for type sharing
---

## Iteration 22 - 5.3 Add export button placeholder
- Added fetchTeamExportData server action to app/(main)/team/actions.ts
- Server action fetches raw log entries with category/subtask joins for the selected date range
- Added "📥 Export Team Report" button at bottom of team page (supervisor view only)
- Button generates a CSV download with columns: Name, Team, Category, Sub-task, Minutes, Occurrences, Method, Note, Date
- CSV uses proper escaping (double-quote wrapping, escaped inner quotes)
- Export respects the currently selected date range preset (This Week/Last Week/This Month)
- Filename includes the date range label (e.g., mpl-team-report-this-week.csv)
- Added toast notifications for export success/error/empty states
- Exporting state disables button and shows "Exporting..." label
- Files changed: app/(main)/team/actions.ts, app/(main)/team/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - CSV generation in-browser using Blob + createObjectURL + programmatic click is clean and avoids needing an API route
  - Server action returns raw rows; CSV formatting happens client-side for proper date localization
  - URL.revokeObjectURL() cleanup prevents memory leaks from blob URLs
  - The ExportRow type is shared between server action and client for type safety
  - Toast pattern reused from log page: setTimeout auto-dismiss, fadeIn animation from globals.css
---

## Iteration 23 - 6.1 Configure PWA manifest and service worker
- Created app/manifest.ts using Next.js built-in MetadataRoute.Manifest (no external PWA package needed)
- Manifest includes: name "MPL", short_name "MPL", theme_color #0EA5E9, background_color #F8FAFC, display standalone, start_url /, 192x192 and 512x512 icons
- Generated PWA icons (192x192, 512x512, apple-touch-icon 180x180) using Python PIL — blue rounded-rect background with white "M" letter
- Created public/sw.js service worker with: precache of main routes/icons on install, stale-while-revalidate for static assets, network-first with cache fallback strategy, old cache cleanup on activate
- Updated app/layout.tsx with: Viewport export (themeColor, width, initialScale, maximumScale), appleWebApp metadata (capable, statusBarStyle, title), apple-touch-icon link, inline script for service worker registration
- Updated next.config.ts with service worker headers: Content-Type, Cache-Control (no-cache), Content-Security-Policy
- Used Next.js built-in PWA support instead of @ducanh2912/next-pwa — cleaner approach for Next.js 16, no extra dependency
- Files changed: app/manifest.ts (new), public/sw.js (new), public/icon-192x192.png (new), public/icon-512x512.png (new), public/apple-touch-icon.png (new), app/layout.tsx, next.config.ts, PRD.md, progress.txt
- Learnings for future iterations:
  - Next.js has built-in manifest support via app/manifest.ts — returns MetadataRoute.Manifest, auto-served at /manifest.webmanifest
  - Viewport config (themeColor) must be exported separately from metadata in Next.js 14+ (separate Viewport export)
  - Service worker registration via inline script with dangerouslySetInnerHTML avoids needing a client component wrapper
  - Service worker headers in next.config.ts ensure sw.js is never cached and has proper content type
  - appleWebApp metadata in Next.js adds necessary iOS PWA meta tags automatically
  - No need for @ducanh2912/next-pwa package — that package is stale (last published ~1 year ago), Next.js built-in approach is preferred for 2025+
---

## Iteration 24 - 6.2 Add install prompt
- Created components/InstallPrompt.tsx as a client component
- Detects PWA installability via `beforeinstallprompt` event
- Shows dismissible banner: "Install MPL for quick access" with Install button and dismiss (✕) button
- Stores dismissal in localStorage (key: mpl-install-dismissed) so banner doesn't reappear
- Checks for standalone display mode — doesn't show if already running as PWA
- Install button calls deferredPrompt.prompt() to trigger native install dialog
- Dismiss button hides banner and persists dismissal to localStorage
- Banner styled with MPL blue tint (bg #E0F2FE, blue border-bottom, blue Install button)
- Added InstallPrompt to app/(main)/layout.tsx between MplHeader and main content
- BeforeInstallPromptEvent interface typed properly with prompt() and userChoice
- Files changed: components/InstallPrompt.tsx (new), app/(main)/layout.tsx
- Learnings for future iterations:
  - beforeinstallprompt event is Chrome/Edge-only (not Safari/Firefox) — the component gracefully handles this by only showing when the event fires
  - Must call e.preventDefault() in the beforeinstallprompt handler to prevent the browser's default mini-infobar
  - window.matchMedia('(display-mode: standalone)') detects if the app is already installed and running as PWA
  - The banner sits between the header and main content in the layout — no fixed positioning needed since it flows naturally
  - localStorage persistence ensures the banner respects user's dismiss choice across sessions
---

## Iteration 25 - 7.1 Add loading states and error handling
- Enhanced skeleton loading states for all 4 pages (log, history, insights, team) to better represent actual content layout
- Added retry buttons to all error states across all pages (log, history, insights, team)
- Error states now show warning icon, styled error message, and a Retry button that reloads the page
- Enhanced empty states with larger icons and better spacing (history, insights, team)
- Team page empty state for filtered members now shows contextual message based on active filter
- Log page: category grid skeletons now match card layout (icon + text + subtext placeholders)
- History page: skeleton shows summary card placeholder + date group header + entry card placeholders
- Insights page: skeleton shows KPI card placeholders + category breakdown chart skeleton
- Team page: skeleton shows gradient aggregate card + filter pill placeholders + member card placeholders
- Files changed: app/(main)/log/page.tsx, app/(main)/history/page.tsx, app/(main)/insights/page.tsx, app/(main)/team/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - window.location.reload() is the simplest retry mechanism for client components that fetch in useEffect — avoids needing to extract fetch logic into a separate callable function
  - Skeleton loaders that mirror the actual layout shape (matching heights, widths, and positions) feel much smoother than generic rectangles
  - Using gradient opacity (from-mpl-primary/20) creates subtle tinted skeleton cards for gradient sections
  - Empty states benefit from slightly larger padding (p-8 vs p-6) and bigger icons (text-3xl) for visual weight
---

## Iteration 26 - 7.2 Mobile responsiveness pass
- Ensured all views work at 320px-430px width range
- Added overflow-x-hidden on html/body and main layout container to prevent horizontal scrolling
- Fixed BottomNav conflicting padding classes (removed duplicate pb-[env(safe-area-inset-bottom)] in favor of separate safe-area div)
- Log page: added min-w-0 and break-words to category cards for long labels, reduced padding to p-3 on narrow screens (sm:p-4), truncate on breadcrumb subtask label, shrink-0 on breadcrumb chevron, smaller time chip padding (px-3 py-1.5), smaller custom input (w-[4.5rem] px-2.5 py-1.5), responsive "How many times?" text (text-xs default, sm:text-sm)
- History page: flex-wrap on category label + badge row, flex-wrap on summary card stats
- Insights page: truncate on category breakdown labels with min-w-0 container, shrink-0 on icon/stats, flex-wrap with gap-y on automation opportunity stats
- Team page: flex-wrap on filter buttons and aggregate card stats, responsive toast width
- Toast notifications: constrained with w-[calc(100%-2rem)] max-w-sm for proper width on narrow screens
- Files changed: app/globals.css, app/(main)/layout.tsx, components/BottomNav.tsx, app/(main)/log/page.tsx, app/(main)/history/page.tsx, app/(main)/insights/page.tsx, app/(main)/team/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - overflow-x-hidden on both html and body is needed to fully prevent horizontal scroll on all browsers
  - flex-wrap is essential for any row of items that might exceed container width (badges, chips, stats)
  - min-w-0 on flex children allows truncate to work — without it, flex items refuse to shrink below content width
  - Toasts need width constraints (calc(100% - 2rem) max-w-sm) to not overflow on narrow screens
  - sm: breakpoint in Tailwind is 640px — for 320px-430px mobile-only range, the base styles ARE the mobile styles
  - break-words on text prevents single long words from causing overflow in constrained containers
---

## Iteration 27 - 7.3 Add page transitions and micro-interactions
- Added slideUp CSS keyframe animation (150ms ease-out, translateY 8px to 0 with opacity)
- Added fillBar CSS keyframe animation for progress bar initial fill effect
- Applied slide-up animation to all 3 log flow step transitions (category, subtask, time entry)
- Applied slide-up animation to history and insights page content on load
- Applied fillBar animation to insights category breakdown progress bars
- Added active:scale-[0.97] press feedback to team page filter buttons (date preset + team filter)
- All animations kept under 200ms per PRD spec
- Toast notifications already had fadeIn animation from previous iterations
- Files changed: app/globals.css, app/(main)/log/page.tsx, app/(main)/history/page.tsx, app/(main)/insights/page.tsx, app/(main)/team/page.tsx, PRD.md, progress.txt
- Learnings for future iterations:
  - Tailwind arbitrary animations: animate-[keyframeName_150ms_ease-out] maps to CSS keyframe animations without needing tailwind.config entries
  - slideUp (translateY 8px → 0 + opacity) is a subtle, fast animation for content appearing — better than slideIn from sides
  - fillBar animation with CSS `from { width: 0% }` lets the bar animate from 0 to its final width set via inline style
  - active:scale-[0.97] is the standard press feedback pattern used across all interactive buttons in the app
  - transition-all vs transition-colors: use transition-all when scale transform is also needed alongside color changes
---
